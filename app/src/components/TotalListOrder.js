import React from 'react'

const TotalListOrder = (props) => {
    this.props.cookList.forEach((food) => {
        console.log(food)
    })
    return (
        <div>Cooks List</div>
    )
}

export default TotalListOrder;